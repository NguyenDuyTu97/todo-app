import React from 'react';
import PropTypes from 'prop-types';

TodoApp.propTypes = {

};

function TodoApp(props) {
    return (
        <div>
            This is todo app
        </div>
    );
}

export default TodoApp;